#include <stdio.h>
#include <stdlib.h>

typedef uint unsigned int;

#define SIZE 10
uint montgomery(uint a, uint b, uint p)
{
  uint r,q;
  uint mask=1;
  for(i=0;i<SIZE;i++)
  {
    r0=r&mask;
    ai=(a>>i)&mask;
    b0=b&mask;
    q=
  }
}

int main(int argc, char** argv)
{
  return EXIT_SUCCESS;
}
